# Custom Theme V2 (Advanced Customization) Plan

## 概要
ユーザーから寄せられた要望に基づき、カスタムテーマ機能を大幅に拡張します。複数のテーマを保存・管理できる「着せ替えギャラリー」機能や、作成中の見た目を確認できる「ライブプレビュー」、画像の細かな調整、フォントやボタン形状の変更など、本格的なカスタマイズアプリへと進化させます。

## 目標 (ユーザー要望)
1. **ガラスの透過度アップ**: 磨りガラス(Blur)だと透過度に限界があるため、純粋な半透明色＋ガラスエッジ（フチ）などの別アプローチでさらに透けるようにする。
2. **ボタン形状のカスタマイズ**: 通常のキーやフリック時のポップアップ形状を変更可能にする。
3. **フォントのカスタマイズ**: 徐々に種類を増やせるよう、フォント選択の基盤を作る。
4. **ライブプレビュー機能**: アプリ側のテーマ作成画面で、実際のキーボードと同じ仕上がりをリアルタイムでプレビューできるようにする。
5. **画像の拡大縮小・位置調整**: 選択した背景画像をトリミング（ピンチズーム・パン）して思い通りの構図で設定できるようにする。
6. **テーマの保存と着せ替え**: 作成したテーマに名前を付けて複数保存し、一覧からワンタップで切り替えられるようにする。

## 実装ステップ提案

### Step 1: テーマ管理基盤の刷新（ギャラリー機能）と透過度再調整
*   `ThemeSettings` に `id`, `name`, `fontName`, `buttonShape` 等のプロパティを追加。
*   AppGroupに保存するデータを「単一のテーマ」から「テーマの配列」と「現在選択されているテーマID」に変更。
*   アプリ側に「マイテーマ一覧（ギャラリー）」画面を作成し、テーマの切り替えや新規作成ができるようにする。
*   ※この段階で、ガラスの透過度を「ブラーなしの純粋な半透明＋エッジ」に変えて透過度を極限まで上げるテストを行う。

### Step 2: ライブプレビュー付きテーマエディタの作成
*   ギャラリーから「新規作成」または「編集」を選んだ際の画面を新造する。
*   画面上半分にダミーのキーボードビュー（FlickKeyboardView等の再利用、またはUIを模したビュー）を配置。
*   下半分に設定項目（画像選択、スタイル、形状、フォント）を配置。
*   設定を変更するたびに上半分のプレビューがリアルタイムで更新されるようにする。

### Step 3: 背景画像のトリミング（拡大縮小・位置調整）機能
*   画像を選択した後、専用のクロップ画面（`UIScrollView` を使ったズーム・パン）に遷移。
*   キーボードの縦横比（大体の比率）に合わせた枠内で画像を調整し、その状態の画像を切り抜いて保存するようにする。

### Step 4: ボタン形状とフォントの拡張
*   **ボタン形状**: 「角丸（現在）」「四角」「丸」「角丸大きめ」などを選択可能にし、キーの `layer.cornerRadius` やフリックビューの描画ロジックに反映。
*   **フォント**: まずは標準の「System」「Rounded」「Serif」「Monospaced」等を追加し、キーボード拡張側でフォントを適用する処理を入れる。

## 注意事項・懸念点
*   **メモリとパフォーマンス**: 画像を複数保存することになるため、テーマごとに一意のファイル名（UUIDなど）で保存し、削除時は画像ファイルも消すなどストレージ管理を徹底する。
*   **UI共有の壁**: プレビュー画面で実際のキーボード（KeyboardExtension内のコード）と全く同じUIを再現するには、ターゲット間でViewのコードをうまく共有するか、プレビュー用のモックを作る必要がある。（`FlickKeyboardView` などを App ターゲットでもコンパイルできるようにするのが理想）
